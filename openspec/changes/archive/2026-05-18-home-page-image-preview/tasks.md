## 1. Add Click-to-Preview JavaScript

- [x] 1.1 Add `extra_js` block to `upload.html` with DOMContentLoaded handler that attaches `openFullscreenPreview()` click events to `.result-card .card-img-top` images
- [x] 1.2 Set `cursor: pointer` and `title: "Click to preview"` on each image

## 2. Testing

- [x] 2.1 Verify clicking a recent upload thumbnail opens the fullscreen preview overlay
- [x] 2.2 Verify hovering shows pointer cursor
- [x] 2.3 Verify existing tests still pass (117 tests)
